import ShopClient from '@/components/ShopClient';

export const metadata = {
  title: 'Shop — BMY Collection',
  description:
    'Shop bespoke and ready-to-wear menswear from BMY Collection, Gombe — caftans, agbada, shadda, yards, vests, trousers, shoes, watches, wristbands, perfumes and caps.'
};

export default function ShopPage() {
  return <ShopClient />;
}
